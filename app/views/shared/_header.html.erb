<div role="navigation" class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle collapsed" type="button">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a href="#" class="navbar-brand">Manage Todo Lists</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="active"><%=link_to "Home", lists_path, :method=>'get' %></li>
            <li><%=link_to "Add New List", new_list_path, :method=>'get' %></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% unless current_user.present? %>
              <li><%=link_to "Signup", new_user_registration_path, :method=>'get' %></li>
              <li><%=link_to "Login", new_user_session_path, :method=>'get' %></li>
            <% else %>
              <li><%=link_to "Logout", destroy_user_session_path, :method=>'delete' %></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
