<center>
	<div class="form_div">
		<h2>Edit Item</h2>

		<%= form_for(@item, url: list_item_path(@list, @item), :method => "put") do |f| %>

		  <div class="form_controls">
			<%= f.label :name %><br />
		  <%= f.text_field :name, autofocus: true %></div>

		  <div class="form_controls"><%= f.label :comment %><br />
			<%= f.text_area :comment, autocomplete: "off" %></div>
		  <div class="form_controls"><%= f.check_box :completed %> <%= f.label :completed %></div>

		  <div class="form_controls"><%= f.submit "Create" , :class=>"btn btn-success" %></div>
		<% end %>
	</div>
</center>
